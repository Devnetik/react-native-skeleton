/**
 * Created by maluramichael on 18/08/16.
 */
const initialState = {
	loadingCount: 0
};

export default reduce = (state, action)=> {
	if (!state) {
		return initialState;
	}

	switch (action.type) {
	}
}